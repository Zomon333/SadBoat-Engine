#pragma once
#include "headers.h"
using namespace std;



int main()
{
	
	/*
	Point A, B, C;
	A = Point(0, 0);
	B = Point(5, 10);
	C = Point(10, 0);

	Line AB, BC, CA;
	AB = Line(A, B);
	BC = Line(B, C);
	CA = Line(A, C);


	Line bounds[3];
	bounds[0] = AB;
	bounds[1] = BC;
	bounds[2] = CA;

	Shape toTest = Shape(bounds);

	Point ptt = Point(6, 5);
	Point upperBound = Point(6, 10000);
	Line raytracer = Line(ptt, upperBound);

	if (raytracer.checkIntersect(AB,TOLERANCE))
		cout << "Collides with AB\n";
	if (raytracer.checkIntersect(BC, TOLERANCE))
		cout << "Collides with BC\n";
	if (raytracer.checkIntersect(CA, TOLERANCE))
		cout << "Collides with CA\n";

		*/

	return 1;
}